import { style } from "@vanilla-extract/css";

export const SidebarMenuContainer = style({
    backgroundColor: "#1c1d20",
    position: "absolute",
    top: 0,
    left: 0,
    width: "100vw",
    height: "100vh",
    zIndex: 9,
});
